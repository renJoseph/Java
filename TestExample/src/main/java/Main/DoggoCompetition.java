package Main;

import java.util.*;

public class DoggoCompetition {
	public static void exclude(int y) {
	//Create list
	List<Integer> places = new ArrayList<>();
	//Create add function		
	
	//add values 1-100 to a list
	for	(int x = 1; x <= 100; x++) {
		if (x != y) {
			places.add(x);
			if (x > 3 && x <21);
			places.
		}
		}
	System.out.println();
	//print list
	}
}

